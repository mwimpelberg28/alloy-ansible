---
# Install with: ansible-galaxy collection install -r requirements.yml -p collections
collections:
  - name: grafana.grafana
    version: ">=6.0.0"
  # Dependencies of the alloy role, pinned to versions compatible with
  # ansible-core 2.16 (required for RHEL 8 / Python 3.6 managed nodes).
  - name: ansible.posix
    version: ">=1.5.0,<2.0.0"
  - name: community.general
    version: ">=8.0.0,<9.0.0"
